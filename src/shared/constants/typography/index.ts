import colors from '../colors';

const fonts = {
  fontFamilyManrope: 'Manrope, Arial, sans-serif',
};

const header = `
font-size: 24px;
font-weight: 700;
font-style: normal;
line-height: normal;
color: ${colors.darkPrimary};
font-family: ${fonts.fontFamilyManrope};
`;

export { fonts, header };
